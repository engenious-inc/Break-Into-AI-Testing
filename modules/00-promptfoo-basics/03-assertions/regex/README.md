# Assertions 2/5 — regex

> **Day 2** · [session index](../../../../days/02-promptfoo-basics.md)

Deterministic `regex` / `not-regex`.

```bash
npx promptfoo@latest eval -c modules/00-promptfoo-basics/03-assertions/regex/promptfooconfig.yaml
```

Always run from the repo root so `.env` is discovered.
