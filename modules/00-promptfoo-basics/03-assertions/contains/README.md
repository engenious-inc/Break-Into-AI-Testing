# Assertions 1/5 — contains

> **Day 2** · [session index](../../../../days/02-promptfoo-basics.md)

Deterministic `icontains` / `contains`.

```bash
npx promptfoo@latest eval -c modules/00-promptfoo-basics/03-assertions/contains/promptfooconfig.yaml
```

Always run from the repo root so `.env` is discovered.
